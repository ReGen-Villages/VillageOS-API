/** The tabs of the temporal page, each named by a sentence under `temporal.tabs`. */
export const TEMPORAL_TABS = ['mutations', 'thingMutations', 'relationshipMutations', 'snapshot', 'propertyHistory', 'stateQuery'] as const;
