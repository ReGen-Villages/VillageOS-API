[pytest]
# Silence a transitive FastAPI/starlette deprecation (httpx vs httpx2) that is
# outside this example's control. Our own code raises no warnings.
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated:
