# Building VillageOS Microservices

1. Create ASP.NET Web API project
2. Add NuGet refs to `vos.Auth.Shared` and `vos.Microservice.Shared`
3. Implement `/handle` endpoint
4. Register with broker on startup
