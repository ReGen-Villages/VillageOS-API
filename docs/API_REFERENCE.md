# VillageOS API Reference

See the Swagger UI at `/swagger` when running the broker.

## Authentication
- JWT: `POST /api/auth/login`
- API Key: `X-Api-Key` header

## SignalR Hub
Connect to `/hubs/vos` for real-time events.
