{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://villageos/contracts/handle-request-metabolism.schema.json",
  "title": "HandleRequestMetabolism",
  "description": "Payload Mycelium POSTs to vos.Service.Metabolism /handle. Shape derived from vos.Service.Metabolism.Models.HandleRequest (record with [System.Text.Json] camelCase wire format).",
  "type": "object",
  "additionalProperties": false,
  "required": ["subjectId", "targetId"],
  "properties": {
    "relationshipId": { "type": ["string", "null"], "description": "Identifier of the relationship being simulated. Nullable per the record signature." },
    "subjectId":      { "type": "string", "minLength": 1, "description": "Subject thing id." },
    "targetId":       { "type": "string", "minLength": 1, "description": "Target thing id." },
    "subjectName":    { "type": ["string", "null"] },
    "targetName":     { "type": ["string", "null"] },
    "properties":     { "description": "Opaque per-mode configuration (JsonElement on the C# side); shape is intentionally not pinned here." }
  }
}
