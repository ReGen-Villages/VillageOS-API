{
  "name": "seed-migrate",
  "version": "1.0.0",
  "private": true,
  "description": "Migrate VillageOS seed files to the Connection/Handler/PrototypeHandler model (Feature #5615).",
  "license": "AGPL-3.0",
  "bin": {
    "seed-migrate": "migrate.js"
  },
  "scripts": {
    "test": "node --test"
  }
}
